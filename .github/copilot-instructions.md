# Copilot Instructions

- This workspace uses Nx with an Angular app named `app` and a NestJS API named `api`.
- Keep changes aligned with the current stack and preserve the monorepo layout.
- Prefer small, focused edits and avoid adding unrelated frameworks.
